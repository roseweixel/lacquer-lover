<div class="well col-lg-6 col-lg-offset-3">
  <legend>
    Write a review for <%= @review.lacquer.name %> (<%= @review.lacquer.brand.name %>)
  </legend>

  <%= render 'form', review: @review %>

</div>
