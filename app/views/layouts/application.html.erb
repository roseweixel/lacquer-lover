<!DOCTYPE html>
<html>
<head>
  <title>Lacquer Love&Lend</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= analytics_init if Rails.env.production? %>
  <link href='https://fonts.googleapis.com/css?family=Leckerli+One' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Anton' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Hind:400,700' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Homemade+Apple' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Karla:400,400italic,700,700italic' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <%= favicon_link_tag 'heart-polish.jpg' %>
</head>
<body>

<div class="container">

  <nav class="nav navbar-default navbar-fixed-top" roll="navigation">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("Lacquer Love&Lend", root_path, class: "navbar-brand navbar-left") %>
    </div>

    <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1" aria-expanded="false" style="height: 1px;">
    
        <% if session[:user_id] %>
          <ul class="nav navbar-nav">
            <li>
              <%= link_to("Welcome, #{current_user.first_name}! See your profile.", user_path(current_user))  %>
            </li> 

            <li><%= link_to("Sign Out", signout_path, id:"sign_out") %></li>

            <li><%= link_to("Find a Friend", users_path) %></li>
          </ul>
        <% else %>
          <ul class="nav navbar-nav">
            <li>
    	    	  <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
            </li>
    <!--         <li>
              <%#= form_for @user, :url => sessions_path, :html => { :style => "padding:15px;" } do |f| %>
                <%#= f.text_field :name, placeholder: "your name" %>
                <%#= f.submit "Sign In" %>

              <%# end %>

            </li> -->
          </ul>
        <% end %>
    </div>

  </nav>
  <div id="messages">
      <% if current_user && !current_user.email %>
        <%= render partial: 'users/email_alert' %>
      <% end %>
    <%= flash_messages %>
  </div>
  <div>
  <%= yield %>

  </div>
</div>
</body>
</html>

<script type="text/javascript">
    window.currentUser = {
        id : "<%=current_user.id if current_user%>"
    }
</script>
