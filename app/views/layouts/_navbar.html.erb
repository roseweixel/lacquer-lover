<nav class="nav navbar navbar-default navbar-fixed-top" roll="navigation">
  <div class="container-fluid">
  
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("Lacquer Love&Lend", root_path, class: "navbar-brand") %>
    </div>

    <div class="navbar-collapse collapse" id="navbar-main">
          <ul class="nav navbar-nav">
            <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Find Lacquer <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li>
                  <%= render 'layouts/lacquer_search_form' %>
                </li>
              </ul>
            </li>
            <% if current_user %>
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Find Lacquer Lover <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li>
                  <%= render 'layouts/friend_search_form' %>
                </li>
              </ul>
              </li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
              <% if session[:user_id] %>
              <li>
                <%= link_to("Welcome, #{current_user.first_name}! See your profile.", user_path(current_user)) %>
              </li>
              <li>
                <%= link_to("Sign Out", signout_path, id:"sign_out") %>
              </li>
              <% else %>
              <li>
                <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
              </li>
              <% end %>
    <!--         <li>
              <%#= form_for @user, :url => sessions_path, :html => { :style => "padding:15px;" } do |f| %>
                <%#= f.text_field :name, placeholder: "your name" %>
                <%#= f.submit "Sign In" %>

              <%# end %>

            </li> -->
          </ul>
        <%# end %>

            

    </div>
  </div>
</nav>
